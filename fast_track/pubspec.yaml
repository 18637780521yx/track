name: fast_track
description: A lightweight Flutter analytics SDK with session management, batching, retry, deduplication, and rate limiting.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fast_tools:
    path:  /Users/fc/fast_utils
  dio: ^5.7.0
  archive: ^4.0.0
  objectbox: ^5.2.0
  objectbox_flutter_libs: ^5.2.0
  path_provider: ^2.1.0
  path: ^1.9.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.2.0
  build_runner: ^2.4.13
  objectbox_generator: ^5.2.0

flutter:
  uses-material-design: false

